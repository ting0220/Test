<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.example.zhaoting.myapplication"
          xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.GET_TASKS"></uses-permission>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".app.ApplicationController"
        android:theme="@style/AppBaseTheme.Light">
        <activity android:name=".view.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".view.article.ArticleCActivity" />
        <activity android:name=".view.article.ThemeArticleCActivity" />
        <activity android:name=".view.comments.CommentActivity" />
        <activity android:name=".view.setting.SettingActivity"/>
    </application>

</manifest>